<Window x:Class="Dashboard107.Client.SecretPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="107 平台验证" Width="520" SizeToContent="Height" MinHeight="220" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="Auto" />
            <RowDefinition Height="12" /><RowDefinition Height="Auto" />
            <RowDefinition Height="16" /><RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" FontSize="14" />
        <TextBlock x:Name="TimeoutText" Grid.Row="2" Foreground="{StaticResource MutedTextBrush}" FontSize="11" />
        <PasswordBox x:Name="SecretBox" Grid.Row="4" KeyDown="SecretBox_KeyDown" />
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="82" Margin="0,0,10,0" IsCancel="True" Content="取消" />
            <Button Width="82" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="Confirm_Click" Content="确认" />
        </StackPanel>
    </Grid>
</Window>
