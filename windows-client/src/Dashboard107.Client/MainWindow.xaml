<Window x:Class="Dashboard107.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="107 Dashboard" Width="1040" Height="690" MinWidth="900" MinHeight="620"
        WindowStartupLocation="CenterScreen" Closing="Window_Closing"
        d:DesignWidth="1040" d:DesignHeight="690">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="*" />
            <RowDefinition Height="52" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="28,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock FontSize="22" FontWeight="SemiBold" Text="107 Dashboard" />
                    <TextBlock Margin="0,4,0,0" Foreground="{StaticResource MutedTextBrush}" Text="Windows 客户端" />
                </StackPanel>
                <Border Grid.Column="1" Padding="12,7" Background="#E6F4F1" CornerRadius="4" VerticalAlignment="Center">
                    <TextBlock x:Name="ConnectionBadge" Foreground="#086B5B" FontWeight="SemiBold" Text="未连接" />
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="28,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.05*" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" /><RowDefinition Height="12" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="10" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="16" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" FontSize="16" FontWeight="SemiBold" Text="平台连接" />
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /><ColumnDefinition Width="14" /><ColumnDefinition Width="118" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Margin="0,0,0,6" Foreground="{StaticResource MutedTextBrush}" Text="SSH 主机" />
                            <TextBox x:Name="HostTextBox" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Margin="0,0,0,6" Foreground="{StaticResource MutedTextBrush}" Text="端口" />
                            <TextBox x:Name="PortTextBox" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Row="4">
                        <TextBlock Margin="0,0,0,6" Foreground="{StaticResource MutedTextBrush}" Text="用户名" />
                        <TextBox x:Name="UsernameTextBox" />
                    </StackPanel>

                    <Grid Grid.Row="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2.2*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Margin="0,0,0,6" Foreground="{StaticResource MutedTextBrush}" Text="SSH 私钥" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="56" />
                                </Grid.ColumnDefinitions>
                                <Border Padding="10,0" Background="#F7F9FA" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="2">
                                    <TextBlock x:Name="PrivateKeyPathTextBlock" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                </Border>
                                <Button Grid.Column="2" Padding="0" ToolTip="选择 SSH 私钥" Click="BrowsePrivateKey_Click" Content="选择" />
                            </Grid>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Margin="0,0,0,6" Foreground="{StaticResource MutedTextBrush}" Text="动态验证码（本次连接）" />
                            <TextBox x:Name="VerificationCodeTextBox" MaxLength="16" ToolTip="只用于本次连接，不会保存" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button x:Name="ConnectButton" Style="{StaticResource PrimaryButton}" Click="ConnectButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="0,0,8,0" FontFamily="Segoe MDL2 Assets" FontSize="15" Text="&#xE774;" />
                                <TextBlock Text="连接并打开平台" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="OpenButton" Grid.Column="2" IsEnabled="False" Click="OpenButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="0,0,8,0" FontFamily="Segoe MDL2 Assets" FontSize="15" Text="&#xE8A7;" />
                                <TextBlock Text="打开网页" />
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Border Grid.Row="10" Background="#F7F9FA" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" MinHeight="132">
                        <TextBox x:Name="ActivityTextBox" Margin="1" Padding="12" MinHeight="128"
                                 FontFamily="Cascadia Mono, Microsoft YaHei UI" FontSize="13" Background="Transparent"
                                 BorderThickness="0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                                 HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
                                 VerticalContentAlignment="Top" />
                    </Border>
                </Grid>
            </Border>

            <StackPanel Grid.Column="2">
                <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Margin="16">
                        <TextBlock FontSize="16" FontWeight="SemiBold" Text="服务状态" />
                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <TextBlock Foreground="{StaticResource MutedTextBrush}" Text="远程版本" />
                            <TextBlock x:Name="RemoteVersionText" Grid.Column="1" HorizontalAlignment="Right" FontWeight="SemiBold" Text="-" />
                        </Grid>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <TextBlock Foreground="{StaticResource MutedTextBrush}" Text="远程端口" />
                            <TextBlock x:Name="RemotePortText" Grid.Column="1" HorizontalAlignment="Right" FontWeight="SemiBold" Text="-" />
                        </Grid>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <TextBlock Foreground="{StaticResource MutedTextBrush}" Text="本地入口" />
                            <TextBlock x:Name="LocalPortText" Grid.Column="1" HorizontalAlignment="Right" FontWeight="SemiBold" Text="-" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Margin="0,20,0,0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Margin="16">
                        <TextBlock FontSize="16" FontWeight="SemiBold" Text="服务操作" />
                        <Button x:Name="UpdateButton" Margin="0,16,0,0" IsEnabled="False" Click="UpdateButton_Click" Content="安装 / 更新" />
                        <Button x:Name="LogsButton" Margin="0,10,0,0" IsEnabled="False" Click="LogsButton_Click" Content="查看服务日志" />
                        <Button x:Name="StopButton" Margin="0,10,0,0" IsEnabled="False" Click="StopButton_Click" Content="停止远程服务" />
                    </StackPanel>
                </Border>

                <Border Margin="0,20,0,0" Padding="12" Background="#FFF8E7" BorderBrush="#E8D59B" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" Foreground="#6C5411" Text="网络" />
                        <TextBlock Margin="0,6,0,0" Foreground="#6C5411" TextWrapping="Wrap" Text="校外连接前请先接入 iWAN 或校园 VPN。" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="28,0">
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}" Text="就绪" />
                <ProgressBar x:Name="ProgressBar" Width="180" Height="5" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed" />
            </Grid>
        </Border>
    </Grid>
</Window>
